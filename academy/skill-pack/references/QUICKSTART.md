# Retired Local Quickstart

This bundled academy quickstart is retired.

Canonical generic runtime docs and examples now live in the marketplace
`intent-space-agent-pack`.

For academy-specific work, use:

1. the canonical marketplace pack
2. `academy/agent-setup.md`
3. `academy/contracts/tutorial-ritual.json`
4. `academy/skill-pack/sdk/promise_runtime.py` only if you fetched the local academy runtime from this host

The only local reference artifact that still matters here is:

- `golden-happy-path.ndjson` for historical debugging
