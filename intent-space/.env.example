# Optional: override data directory (default: ~/.differ/intent-space/)
# DIFFER_INTENT_SPACE_DIR=/path/to/data

# Optional: enable TCP listener
# INTENT_SPACE_PORT=4000
# INTENT_SPACE_HOST=0.0.0.0

# Optional: override agent identity (default: intent-space)
# DIFFER_INTENT_SPACE_ID=intent-space
